"use strict";

// changing the case - toUpperCase / toLowerCase
let str = "Interface";

alert( str.toUpperCase() ); // INTERFACE
alert( str.toLowerCase() ); // interface

alert( str[0].toLowerCase() ); // i, only the first character is changed