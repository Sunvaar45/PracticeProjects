"use strict";

/* Lexical environment */
// during function call:
    // inner lexical environment = function call - function args, variables inside
    // outer lexical environment = global - function itself, variables outside
    // inner le has a referance to the outer le